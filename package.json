{
  "dependencies": {
    "discord.js": "^14.14.1",
    "dotenv": "^16.4.5",
    "wio.db": "^4.0.22"
  },
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "start": "rm -rf dist && npx tsc && node dist/main.js"
  },
  "devDependencies": {
    "tsc": "^2.0.4",
    "typescript": "^5.3.3"
  }
}
